import SideNav from '@/components/ui/dashboard/SideNav';
import * as React from 'react';

interface IPageProps {
}

const Page: React.FunctionComponent<IPageProps> = (props) => {
  return (<>
    <div>project Navigator</div>
  </>);
};

export default Page;
