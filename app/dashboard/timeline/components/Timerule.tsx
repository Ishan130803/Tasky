import React from 'react'

export default function Timerule(props:{className:String}) {
  return (
    <div className={`bg-white h-10 drop-shadow-lg ${props.className}`}>
      
    </div>
  )
}
